<h2 class="pageTitle">Trips</h2>
<div id="trips">
  <% if @trips.any? %>
    <%= render(partial: 'trip', collection: @trips.to_a, as: :trip) %>

<% if @pagination.any? %>
<div class="apple_pagination">
  <%= will_paginate(@pagination) %>
</div>
<% end %>
<% end %>
</div>
